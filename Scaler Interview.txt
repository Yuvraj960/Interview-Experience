1. Coding & Python Scripting:
How would you determine the number of distinct words in a file while reading and counting them?

In your Python word-counting logic, what exactly are you incrementing, and how does that incrementation occur?

What is the significance of the if statement (and converting words to lowercase) in your word-counting logic?

2. Docker & Containerization:
What is the difference between Docker and a Virtual Machine (VM)?

(Contextual Correction) You mentioned writing a Dockerfile to manage communication between three containers; isn't that actually the purpose of a Docker Compose file?

3. Kubernetes & Architecture:
What is the architecture of Kubernetes, and how do its core components work?

How does Kubernetes handle container orchestration and auto-healing?

4. Systems Monitoring & Observability:
How can an application (particularly in a small startup context) be monitored so that developers are informed of crashes or issues without relying on customer reports?

How do you detect if a specific Docker container has died, or how do you examine container-related logs?

If an application inside a container crashes but the Kubernetes pod itself remains alive ("Running"), how would you monitor and detect that specific failure?

5. Computer Networks & Core Protocols:
What exact infrastructure and step-by-step process occurs when a user accesses "google.com" via a browser or terminal?

How is an IP address fetched during a DNS query, and how does the central DNS mapping work globally?

What are IP tables, and what is their actual purpose in a Linux/networking system?

In the TCP three-way handshake, why does the server send a random sequence number and a SYN flag in its SYN-ACK response?

What is DNS caching, and what potential problems (such as stale data) can arise from it?

What does the padlock symbol visible in a web browser represent?

What is the benefit of HTTPS over HTTP, and how does the underlying encryption mechanism in HTTPS actually work?

6. AWS & Cloud Infrastructure:
How do you securely connect and allow communication between instances located in two different VPCs (Virtual Private Clouds) without exposing internal traffic to the public internet? (Topic: VPC Peering)

What is the primary function of an Amazon EC2 instance?

What are the different types of EC2 instances, and what factors determine their cost segregation?